describe('daffApplyRequestToFilters', () => {
  it('should apply a request', () => {

  });

  it('should apply a toggle request', () => {

  });

  it('should not apply a request that does not match a known filter name', () => {

  });

  it('should throw an error if a request name matches a filter name, but is the wrong type', () => {

  });
});
