import { InjectionToken } from '@angular/core';

/**
 * Injection token that serves as a placeholder for any service that implements the DaffAuthTransformerInterface.
 */
export const DaffAuthTransformer =
    new InjectionToken('DaffAuthTransformer');
