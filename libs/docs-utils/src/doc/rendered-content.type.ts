/**
 * A marker type that denontes a field contains rendered content.
 * This content is rendered as HTML as should be displayed with `innerHTML` or `outerHTML`.
 */
export type DaffDocsRenderedContent = string;
