/**
 * A reference to a different doc.
 */
export interface DaffDocsApiRef {
  /**
   * The human-readable label.
   */
  label: string;
  /**
   * The path to the referenced doc.
   */
  path: string;
}
