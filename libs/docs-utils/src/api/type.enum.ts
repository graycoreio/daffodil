/**
 * The type of symbol that an API doc contains.
 */
export enum DaffDocsApiType {
  PACKAGE = 'package',
  CLASS = 'class',
  INTERFACE = 'interface',
  TYPE_ALIAS = 'type-alias',
  CONST = 'const',
  FUNCTION = 'function',
  ENUM = 'enum',
}
