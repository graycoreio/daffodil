export enum DaffProductCustomAttributeKind {
  /**
   * A custom attribute that has a scalar value, i.e a number or text.
   */
  SCALAR = 'scalar',
  /**
   * A custom attribute whose value is selected from a list of predefined options.
   */
  SELECT = 'select'
}
