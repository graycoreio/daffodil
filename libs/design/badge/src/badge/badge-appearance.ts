/**
 * The visual style of a badge.
 *
 * - `filled` renders a solid background (the default).
 * - `outlined` renders a semi-transparent background with a colored border.
 */
export type DaffBadgeAppearance = 'filled' | 'outlined';
