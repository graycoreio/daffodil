import {
  waitForAsync,
  TestBed,
} from '@angular/core/testing';


describe('@daffodil/design | DaffTextareaComponent | Default', () => {
  beforeEach(waitForAsync(() => {
    TestBed.configureTestingModule({
    })
      .compileComponents();
  }));
});
