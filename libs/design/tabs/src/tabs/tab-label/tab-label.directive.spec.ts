import { DaffTabLabelDirective } from './tab-label.directive';

describe('DaffTabLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new DaffTabLabelDirective();
    expect(directive).toBeTruthy();
  });
});
