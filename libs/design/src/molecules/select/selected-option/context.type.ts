/**
 * The slot context for {@link DaffSelectSelectedOptionDirective}.
 */
export interface DaffSelectSelectedOptionDirectiveContext<T = unknown> {
  /**
   * The currently selected option, if there is one.
   */
  selected?: T;
}
