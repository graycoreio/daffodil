import { DaffSidebarViewportContentDirective } from './sidebar-viewport-content.directive';

describe('DaffSidebarViewportContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DaffSidebarViewportContentDirective();
    expect(directive).toBeTruthy();
  });
});
