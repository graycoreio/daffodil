import { DaffMediaDirective } from './media.directive';

describe('DaffMediaDirective', () => {
  it('should create an instance', () => {
    const directive = new DaffMediaDirective();
    expect(directive).toBeTruthy();
  });
});
