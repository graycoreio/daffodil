import { DaffHeroSubtitleDirective } from './hero-subtitle.directive';

describe('DaffHeroSubtitleDirective', () => {
  it('should create an instance', () => {
    const component = new DaffHeroSubtitleDirective();
    expect(component).toBeTruthy();
  });
});
