import { DaffHeroSubtitleComponent } from './hero-subtitle.component';

describe('DaffHeroSubtitleComponent', () => {
  it('should create an instance', () => {
    const component = new DaffHeroSubtitleComponent();
    expect(component).toBeTruthy();
  });
});
