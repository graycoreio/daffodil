import { DaffHeroTitleComponent } from './hero-title.component';

describe('DaffHeroTitleComponent', () => {
  it('should create an instance', () => {
    const component = new DaffHeroTitleComponent();
    expect(component).toBeTruthy();
  });
});
