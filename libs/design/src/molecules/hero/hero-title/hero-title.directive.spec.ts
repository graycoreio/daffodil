import { DaffHeroTitleDirective } from './hero-title.directive';

describe('DaffHeroTitleDirective', () => {
  it('should create an instance', () => {
    const component = new DaffHeroTitleDirective();
    expect(component).toBeTruthy();
  });
});
