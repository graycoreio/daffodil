{
  "$schema": "http://json-schema.org/schema",
  "$id": "DaffodilNgAdd",
  "title": "Daffodil ng-add Options Schema",
  "type": "object",
  "description": "Adds Daffodil commerce demo to an existing Angular project",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add Daffodil to",
      "$default": {
        "$source": "projectName"
      }
    },
    "skipPackageJson": {
      "type": "boolean",
      "description": "Skip updating package.json dependencies",
      "default": false
    },
    "isNewProject": {
      "type": "boolean",
      "description": "Is this a new Angular project?",
      "x-prompt": "Is this a new Angular project?"
    },
    "driver": {
      "type": "string",
      "description": "Which driver do you want to use?",
      "enum": [
        "magento",
        "shopify",
        "in-memory",
        "demo"
      ],
      "x-prompt": {
        "message": "Which driver do you want to use?",
        "type": "list",
        "items": [
          {
            "value": "magento",
            "label": "Magento"
          },
          {
            "value": "shopify",
            "label": "Shopify"
          },
          {
            "value": "in-memory",
            "label": "In-Memory (for testing)"
          },
          {
            "value": "demo",
            "label": "Demo (Swapping between drivers)"
          }
        ]
      }
    },
    "magentoVersion": {
      "type": "string",
      "description": "Which version of the Magento API does your store use? Only applies when driver is 'magento'. Supported values: 2.4.1, 2.4.2, 2.4.3",
      "enum": [
        "2.4.1",
        "2.4.2",
        "2.4.3"
      ],
      "default": "2.4.3"
    }
  }
}